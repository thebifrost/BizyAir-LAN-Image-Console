# 安全策略

## 支持范围

当前项目默认支持本机或可信局域网部署，不按公网多租户 SaaS 的安全模型设计。

如果需要公网访问，请至少额外配置：

- HTTPS
- 反向代理
- 防火墙或访问控制
- 强随机 `ADMIN_TOKEN`
- 独立运行用户和最小文件权限
- 日志与数据目录的访问隔离

## 敏感信息

请不要在 Issue、PR、截图或日志中公开以下内容：

- `.env`
- BizyAir API Key
- OpenAI-compatible provider API Key
- `ADMIN_TOKEN`
- `data/` 中的任务数据库或输入/结果图片
- `logs/` 中的运行日志和审计日志

## 报告漏洞

如果你发现安全问题，请不要先公开复现细节。建议先通过项目维护者公开的联系方式私下报告，并包含：

- 影响版本或提交
- 复现步骤
- 可能影响
- 建议修复方向

## 设计边界

项目已包含一些局域网场景下的安全措施，例如 Bearer Token 鉴权、敏感日志脱敏、静态文件路径限制、远程图片缓存的私网地址拦截和大小限制。

这些措施不能替代公网部署所需的完整安全边界。
